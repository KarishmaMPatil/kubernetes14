#LOADBALNCER SERVICE
apiVersion: v1 
metadata: 
      name: pod-lb
      labels:
        app: podlb  #tags  
kind:  Pod
spec: 
     containers:
      - name: nginx-container 
        image: nginx
        ports:
          - containerPort: 80

--- 
apiVersion: v1
metadata: 
    name: lb-service
kind: Service
spec: 
    selector:
        app: podlb
    type: LoadBalancer
    ports:
      - protocol: TCP 
        port: 80 
        targetPort: 80
        

 #kubectl expose pod cipod  --port=80 --target-port=80 --type=ClusterIP
