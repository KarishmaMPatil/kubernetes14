#NODEPORT SERVICE
apiVersion: v1 
metadata: 
      name: pod-np 
      labels:
        app: podnp  #tags  
kind:  Pod
spec: 
     containers:
      - name: nginx-container 
        image: nginx
        ports:
          - containerPort: 80

--- 
apiVersion: v1
metadata: 
    name: np-service
kind: Service
spec: 
    selector:
        app: podnp
    type: NodePort
    ports:
      - protocol: TCP 
        port: 80 
        targetPort: 80
        

 #kubectl expose pod cipod  --port=80 --target-port=80 --type=ClusterIP
